package pk;

// create a public enum called Faces with the following values
// Values are used in tandem with the Dice class for rolling
public enum Faces {
    MONKEY, PARROT, GOLD, DIAMOND, SABER, SKULL
}

